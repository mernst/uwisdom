# To build the CodeRabbit CLI:
curl -fsSL https://cli.coderabbit.ai/install.sh | sh
coderabbit auth login

# To update:
coderabbit update

# To review, either of these:
coderabbit --prompt-only
cr --prompt-only
